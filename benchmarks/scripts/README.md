# Benchmark Scripts

Scripts in this directory will eventually manage environment capture, workload
invocation, and result normalization. Phase-00 keeps the directory documented
without shipping misleading runner logic.

